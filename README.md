# Digi-Clock

Digi Clock is a simple digital clock built using HTML, CSS, and JavaScript.  
It displays time down to the last second.

Features
 Has AM/PM cycles
 cool glow design

How It Works
The clock uses JavaScript's `Date()` object to get the time, then turns it into a 12-hour layout, and updates every second using `setInterval()`.
